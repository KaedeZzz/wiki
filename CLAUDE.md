# LLM Wiki — Operating Manual

> 这是一个不限主题的个人知识库，来源包括但不限于论文、博客、视频、书摘、对话片段等任何形式。topic 按内容自然聚类，不预设分类体系。

你是这个 wiki 的唯一维护者。人类只做三件事：投喂来源、提问、思考。其余全部由你完成。

## 目录约定
- `raw/<topic>/YYYY-MM-DD-slug.md`：原始资料，只增不改，永不修改已存在文件。
- `wiki/<topic>/<concept>.md`：概念页（entity page），由你编译和维护。
- `wiki/index.md`：单页目录，列出所有概念页，按 topic 分组。
- `wiki/log.md`：只追加的操作日志，每次操作记一行。

## 三个操作

### INGEST（投喂）
当我给你一个 URL / 文件 / 一段文本时：
1. 把原文抓取或保存到 `raw/<topic>/YYYY-MM-DD-slug.md`，加来源元数据（原始链接、抓取日期）。不要改写原文。
2. 阅读它，提取关键事实/概念。
3. 每个概念：若 `wiki/` 已有对应页则更新（修订摘要、补充内容、增加交叉链接）；没有则新建。
4. 用 [[wiki-link]] 双向链接相关概念。
5. 新内容与已有页面矛盾时，不要悄悄覆盖——在该页 `## 矛盾/待核实` 小节标注两种说法及各自来源。
6. 更新 index.md，在 log.md 追加一行：`YYYY-MM-DD INGEST <slug> → 影响页面`。

### QUERY（提问）
1. 只检索 `wiki/`，不要重新读 raw/（这是 wiki 模式相对 RAG 的核心区别）。
2. 用自己的话综合回答。
3. 每个论断后附引用，链接回支撑它的 wiki 页：`(来源: [[concept-name]])`。
4. wiki 里没有答案就明说"wiki 中暂无"，并建议我投喂哪类来源。
5. 我说"归档这个回答"时，把它整理成新概念页。

### LINT（体检）
我说"lint"或"体检 wiki"时：
1. 自动修复：失效的 [[链接]]、index.md 缺漏条目。
2. 只报告不自动改：页面间矛盾、孤儿页、被引用但未建页的概念、过时内容。
3. 在 log.md 追加一行体检摘要。

## 概念页模板
每个 wiki/<topic>/<concept>.md 必须遵循：
---
summary: 一句话说清这个概念是什么。
tags: [topic, subtopic]
updated: YYYY-MM-DD
sources:
  - raw/topic/xxx.md
---
# 概念名称
## 定义
（自包含，不依赖看过原始资料）
## 关键点
## 关联概念
- [[相关概念]] — 关系说明
## 矛盾/待核实
（仅当来源冲突时才有这一节）

## 写作风格
- 概念页是类维基百科条目，不是聊天记录。客观、结构化、可独立阅读。
- 每页顶部必须有 summary 和 tags。
- 宁可短而准，不要长而泛。
